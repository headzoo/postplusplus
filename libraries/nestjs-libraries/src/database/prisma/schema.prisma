generator client {
  provider = "prisma-client-js"
  runtime  = "nodejs"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Organization {
  id                                  String                               @id @default(uuid())
  name                                String
  description                         String?
  apiKey                              String?
  paymentId                           String?
  streakSince                         DateTime?
  createdAt                           DateTime                             @default(now())
  updatedAt                           DateTime                             @updatedAt
  allowTrial                          Boolean                              @default(false)
  isTrailing                          Boolean                              @default(false)
  shortlink                           ShortLinkPreference                  @default(ASK)
  autoPost                            AutoPost[]
  Comments                            Comments[]
  credits                             Credits[]
  customers                           Customer[]
  errors                              Errors[]
  github                              GitHub[]
  Integration                         Integration[]
  media                               Media[]
  buyerOrganization                   MessagesGroup[]
  notifications                       Notifications[]
  plugs                               Plugs[]
  pipelinePlugs                       PipelinePlug[]
  post                                Post[]                               @relation("organization")
  submittedPost                       Post[]                               @relation("submittedForOrg")
  sets                                Sets[]
  signatures                          Signatures[]
  subscription                        Subscription?
  tags                                Tags[]
  thirdParty                          ThirdParty[]
  usedCodes                           UsedCodes[]
  users                               UserOrganization[]
  webhooks                            Webhooks[]
  oauthApp                            OAuthApp[]
  oauthAuthorizations                 OAuthAuthorization[]
  pipelines                           Pipeline[]
  contextDocuments                    ContextDocument[]
  channelAudienceMembers              ChannelAudienceMember[]
  channelInteractionEvents            ChannelInteractionEvent[]
  channelInteractionDailyAggregates   ChannelInteractionDailyAggregate[]
  channelInteractionWindowSummaries   ChannelInteractionWindowSummary[]
  channelInteractionRollupStates      ChannelInteractionRollupState[]
  channelInteractionSubscriptions     ChannelInteractionSubscription[]
  channelInteractionAuthorizations    ChannelInteractionAuthorization[]
  channelFollowerSyncStates           ChannelFollowerSyncState[]
  channelAudienceNotes                ChannelAudienceNote[]
  channelAudienceLists                ChannelAudienceList[]
  channelAudienceListMembers          ChannelAudienceListMember[]
  channelAudienceMemberTriageIgnores  ChannelAudienceMemberTriageIgnore[]
  channelAudienceMemberGrades         ChannelAudienceMemberGrade[]
  channelAudienceLeadFitFeedbacks     ChannelAudienceLeadFitFeedback[]
  channelRelationshipGradeSnapshots   ChannelRelationshipGradeSnapshot[]
  channelAudienceLeadBridges          ChannelAudienceLeadBridge[]
  channelAudienceCultivatePickBatches ChannelAudienceCultivatePickBatch[]
  channelAudienceCultivatePicks       ChannelAudienceCultivatePick[]
  channelAudienceHotPickBatches       ChannelAudienceHotPickBatch[]
  channelAudienceHotPicks             ChannelAudienceHotPick[]
  channelAnalyticsDailyPoints         ChannelAnalyticsDailyPoint[]
  channelAnalyticsPostMetricSnapshots ChannelAnalyticsPostMetricSnapshot[]
  channelAnalyticsSyncStates          ChannelAnalyticsSyncState[]
  postHttpLogs                        PostHttpLog[]
  webhookHttpLogs                     WebhookHttpLog[]
  dashboardAnalyticsPreferences       DashboardAnalyticsPreference[]
  postRules                           PostRule[]
  postRuleIntegrations                PostRuleIntegration[]
  postRulePipelines                   PostRulePipeline[]
  postRuleRuns                        PostRuleRun[]
  postRuleEvaluations                 PostRuleEvaluation[]

  @@index([apiKey])
  @@index([streakSince])
  @@index([paymentId])
}

model Tags {
  id           String       @id @default(uuid())
  name         String
  color        String
  orgId        String
  deletedAt    DateTime?
  createdAt    DateTime     @default(now())
  updatedAt    DateTime     @updatedAt
  organization Organization @relation(fields: [orgId], references: [id])
  posts        TagsPosts[]

  @@index([orgId])
  @@index([deletedAt])
}

model TagsPosts {
  postId    String
  tagId     String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  post      Post     @relation(fields: [postId], references: [id])
  tag       Tags     @relation(fields: [tagId], references: [id])

  @@id([postId, tagId])
  @@unique([postId, tagId])
}

model User {
  id                                 String                              @id @default(uuid())
  email                              String
  password                           String?
  providerName                       Provider
  name                               String?
  lastName                           String?
  isSuperAdmin                       Boolean                             @default(false)
  bio                                String?
  audience                           Int                                 @default(0)
  pictureId                          String?
  providerId                         String?
  timezone                           Int
  createdAt                          DateTime                            @default(now())
  updatedAt                          DateTime                            @updatedAt
  lastReadNotifications              DateTime                            @default(now())
  inviteId                           String?
  activated                          Boolean                             @default(true)
  account                            String?
  connectedAccount                   Boolean                             @default(false)
  lastOnline                         DateTime                            @default(now())
  ip                                 String?
  agent                              String?
  comments                           Comments[]
  items                              ItemUser[]
  groupBuyer                         MessagesGroup[]                     @relation("groupBuyer")
  groupSeller                        MessagesGroup[]                     @relation("groupSeller")
  orderBuyer                         Orders[]                            @relation("orderBuyer")
  orderSeller                        Orders[]                            @relation("orderSeller")
  payoutProblems                     PayoutProblems[]
  agencies                           SocialMediaAgency?
  picture                            Media?                              @relation(fields: [pictureId], references: [id])
  organizations                      UserOrganization[]
  sendSuccessEmails                  Boolean                             @default(true)
  sendFailureEmails                  Boolean                             @default(true)
  sendStreakEmails                   Boolean                             @default(true)
  oauthAuthorizations                OAuthAuthorization[]
  integrationNoticeReads             IntegrationNoticeRead[]
  channelAudienceNotes               ChannelAudienceNote[]
  channelAudienceLists               ChannelAudienceList[]
  channelAudienceMemberGrades        ChannelAudienceMemberGrade[]
  channelAudienceMemberTriageIgnores ChannelAudienceMemberTriageIgnore[]
  channelAudienceLeadFitFeedbacks    ChannelAudienceLeadFitFeedback[]
  channelAudienceMembersIgnored      ChannelAudienceMember[]             @relation("AudienceMemberIgnoredBy")
  adminPasskeyCredentials            AdminPasskeyCredential[]
  adminWebAuthnChallenges            AdminWebAuthnChallenge[]
  adminVerificationSessions          AdminVerificationSession[]
  dashboardAnalyticsPreferences      DashboardAnalyticsPreference[]
  dismissedAlerts                    UserDismissedAlert[]

  @@unique([email, providerName])
  @@index([lastReadNotifications])
  @@index([inviteId])
  @@index([account])
  @@index([lastOnline])
  @@index([pictureId])
}

model AdminPasskeyCredential {
  id                   String                     @id @default(uuid())
  userId               String
  credentialId         String                     @unique
  publicKey            Bytes
  counter              BigInt
  transports           Json?
  deviceType           String
  backedUp             Boolean
  aaguid               String?
  name                 String?
  createdAt            DateTime                   @default(now())
  updatedAt            DateTime                   @updatedAt
  lastUsedAt           DateTime?
  revokedAt            DateTime?
  user                 User                       @relation(fields: [userId], references: [id], onDelete: Cascade)
  verificationSessions AdminVerificationSession[]

  @@index([userId])
  @@index([userId, revokedAt])
}

model AdminWebAuthnChallenge {
  id        String                     @id @default(uuid())
  userId    String
  challenge String                     @unique
  kind      AdminWebAuthnChallengeKind
  expiresAt DateTime
  createdAt DateTime                   @default(now())
  usedAt    DateTime?
  user      User                       @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, kind, expiresAt])
  @@index([expiresAt])
}

model AdminVerificationSession {
  id              String                  @id @default(uuid())
  userId          String
  credentialId    String?
  tokenHash       String                  @unique
  authenticatedAt DateTime                @default(now())
  expiresAt       DateTime
  revokedAt       DateTime?
  createdAt       DateTime                @default(now())
  updatedAt       DateTime                @updatedAt
  user            User                    @relation(fields: [userId], references: [id], onDelete: Cascade)
  credential      AdminPasskeyCredential? @relation(fields: [credentialId], references: [id], onDelete: SetNull)

  @@index([userId, expiresAt])
  @@index([userId, revokedAt])
  @@index([credentialId])
}

enum AdminWebAuthnChallengeKind {
  REGISTRATION
  AUTHENTICATION
}

model UsedCodes {
  id           String       @id @default(uuid())
  code         String
  orgId        String
  createdAt    DateTime     @default(now())
  updatedAt    DateTime     @updatedAt
  organization Organization @relation(fields: [orgId], references: [id])

  @@index([code])
}

model UserOrganization {
  id             String       @id @default(uuid())
  userId         String
  organizationId String
  disabled       Boolean      @default(false)
  role           Role         @default(USER)
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id])
  user           User         @relation(fields: [userId], references: [id])

  @@unique([userId, organizationId])
  @@index([disabled])
}

model GitHub {
  id             String       @id @default(uuid())
  login          String?
  name           String?
  token          String
  jobId          String?
  organizationId String
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([login])
  @@index([organizationId])
}

model Trending {
  id           String   @id @default(uuid())
  trendingList String
  language     String?  @unique
  hash         String
  date         DateTime
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@index([hash])
}

model TrendingLog {
  id       String   @id @default(uuid())
  language String?
  date     DateTime
}

model ItemUser {
  id     String @id @default(uuid())
  userId String
  key    String
  user   User   @relation(fields: [userId], references: [id])

  @@unique([userId, key])
  @@index([userId])
  @@index([key])
}

model Star {
  id         String   @id @default(uuid())
  stars      Int
  totalStars Int
  forks      Int
  totalForks Int
  login      String
  date       DateTime @default(now()) @db.Date
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@unique([login, date])
}

model Media {
  id                 String              @id @default(uuid())
  name               String
  originalName       String?
  path               String
  organizationId     String
  createdAt          DateTime            @default(now())
  updatedAt          DateTime            @updatedAt
  deletedAt          DateTime?
  fileSize           Int                 @default(0)
  type               String              @default("image")
  thumbnail          String?
  alt                String?
  thumbnailTimestamp Int?
  hidden             Boolean             @default(false)
  organization       Organization        @relation(fields: [organizationId], references: [id])
  agencies           SocialMediaAgency[]
  userPicture        User[]
  oauthApps          OAuthApp[]

  @@index([name])
  @@index([organizationId])
  @@index([type])
}

model SocialMediaAgency {
  id               String                   @id @default(uuid())
  userId           String                   @unique
  name             String
  logoId           String?
  website          String?
  slug             String?
  facebook         String?
  instagram        String?
  twitter          String?
  linkedIn         String?
  youtube          String?
  tiktok           String?
  otherSocialMedia String?
  shortDescription String
  description      String
  approved         Boolean                  @default(false)
  createdAt        DateTime                 @default(now())
  updatedAt        DateTime                 @updatedAt
  deletedAt        DateTime?
  logo             Media?                   @relation(fields: [logoId], references: [id])
  user             User                     @relation(fields: [userId], references: [id])
  niches           SocialMediaAgencyNiche[]

  @@index([userId])
  @@index([deletedAt])
  @@index([id])
}

model SocialMediaAgencyNiche {
  agencyId String
  niche    String
  agency   SocialMediaAgency @relation(fields: [agencyId], references: [id])

  @@id([agencyId, niche])
}

model Credits {
  id             String       @id @default(uuid())
  organizationId String
  credits        Int
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  type           String       @default("ai_images")
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([createdAt])
}

model Subscription {
  id               String           @id @default(cuid())
  organizationId   String           @unique
  subscriptionTier SubscriptionTier
  identifier       String?
  cancelAt         DateTime?
  period           Period
  totalChannels    Int
  isLifetime       Boolean          @default(false)
  createdAt        DateTime         @default(now())
  updatedAt        DateTime         @updatedAt
  deletedAt        DateTime?
  organization     Organization     @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
  @@index([deletedAt])
}

model Customer {
  id           String        @id @default(uuid())
  name         String
  orgId        String
  position     Int           @default(0)
  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @updatedAt
  deletedAt    DateTime?
  organization Organization  @relation(fields: [orgId], references: [id])
  integrations Integration[]

  @@unique([orgId, name, deletedAt])
}

model Integration {
  id                                  String                               @id @default(cuid())
  internalId                          String
  organizationId                      String
  name                                String
  picture                             String?
  providerIdentifier                  String
  type                                String
  token                               String
  disabled                            Boolean                              @default(false)
  tokenExpiration                     DateTime?
  refreshToken                        String?
  profile                             String?
  deletedAt                           DateTime?
  createdAt                           DateTime                             @default(now())
  updatedAt                           DateTime?                            @updatedAt
  inBetweenSteps                      Boolean                              @default(false)
  refreshNeeded                       Boolean                              @default(false)
  postingTimes                        String                               @default("[{\"time\":120}, {\"time\":400}, {\"time\":700}]")
  customInstanceDetails               String?
  customerId                          String?
  rootInternalId                      String?
  additionalSettings                  String?                              @default("[]")
  strategyId                          String                               @default("grow_audience")
  strategyVersion                     Int                                  @default(1)
  utmParams                           String?
  exisingPlugData                     ExisingPlugData[]
  customer                            Customer?                            @relation(fields: [customerId], references: [id])
  organization                        Organization                         @relation(fields: [organizationId], references: [id])
  webhooks                            IntegrationsWebhooks[]
  orderItems                          OrderItems[]
  plugs                               Plugs[]
  pipelinePlugs                       PipelinePlug[]
  posts                               Post[]
  pipelineIntegrations                PipelineIntegration[]
  noticeReads                         IntegrationNoticeRead[]
  channelAudienceMembers              ChannelAudienceMember[]
  channelInteractionEvents            ChannelInteractionEvent[]
  channelInteractionDailyAggregates   ChannelInteractionDailyAggregate[]
  channelInteractionWindowSummaries   ChannelInteractionWindowSummary[]
  channelInteractionRollupStates      ChannelInteractionRollupState[]
  channelInteractionSubscriptions     ChannelInteractionSubscription[]
  channelInteractionAuthorization     ChannelInteractionAuthorization?
  channelFollowerSyncState            ChannelFollowerSyncState?
  channelAudienceNotes                ChannelAudienceNote[]
  channelAudienceLists                ChannelAudienceList[]
  channelAudienceListMembers          ChannelAudienceListMember[]
  channelAudienceMemberTriageIgnores  ChannelAudienceMemberTriageIgnore[]
  channelAudienceMemberGrades         ChannelAudienceMemberGrade[]
  channelAudienceLeadFitFeedbacks     ChannelAudienceLeadFitFeedback[]
  channelRelationshipGradeSnapshots   ChannelRelationshipGradeSnapshot[]
  channelAudienceLeadBridges          ChannelAudienceLeadBridge[]
  channelAudienceCultivatePickBatches ChannelAudienceCultivatePickBatch[]
  channelAudienceCultivatePicks       ChannelAudienceCultivatePick[]
  channelAudienceHotPickBatches       ChannelAudienceHotPickBatch[]
  channelAudienceHotPicks             ChannelAudienceHotPick[]
  channelAnalyticsDailyPoints         ChannelAnalyticsDailyPoint[]
  channelAnalyticsPostMetricSnapshots ChannelAnalyticsPostMetricSnapshot[]
  channelAnalyticsSyncState           ChannelAnalyticsSyncState?
  postHttpLogs                        PostHttpLog[]
  webhookHttpLogs                     WebhookHttpLog[]
  contextDocuments                    IntegrationContextDocument[]
  dashboardAnalyticsPreferences       DashboardAnalyticsPreference[]
  postRuleIntegrations                PostRuleIntegration[]

  @@unique([organizationId, internalId])
  @@index([rootInternalId])
  @@index([organizationId])
  @@index([providerIdentifier])
  @@index([updatedAt])
  @@index([createdAt])
  @@index([deletedAt])
  @@index([customerId])
  @@index([inBetweenSteps])
  @@index([refreshNeeded])
  @@index([disabled])
}

model IntegrationNoticeRead {
  userId        String
  integrationId String
  lastReadAt    DateTime    @default(now())
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt
  user          User        @relation(fields: [userId], references: [id])
  integration   Integration @relation(fields: [integrationId], references: [id])

  @@id([userId, integrationId])
  @@unique([userId, integrationId])
  @@index([userId])
  @@index([integrationId])
  @@index([lastReadAt])
}

model DashboardAnalyticsPreference {
  id             String       @id @default(cuid())
  userId         String
  organizationId String
  integrationId  String
  metricKey      String
  position       Int
  hidden         Boolean      @default(false)
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  user           User         @relation(fields: [userId], references: [id], onDelete: Cascade)
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  integration    Integration  @relation(fields: [integrationId], references: [id], onDelete: Cascade)

  @@unique([userId, organizationId, integrationId, metricKey])
  @@index([userId, organizationId])
  @@index([organizationId, integrationId])
  @@index([integrationId])
}

model UserDismissedAlert {
  id          String   @id @default(uuid())
  userId      String
  alertKey    String
  dismissedAt DateTime @default(now())
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, alertKey])
  @@index([userId])
}

model Signatures {
  id             String       @id @default(uuid())
  organizationId String
  content        String
  autoAdd        Boolean
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([createdAt])
  @@index([organizationId])
  @@index([deletedAt])
}

model Comments {
  id             String       @id @default(uuid())
  content        String
  organizationId String
  postId         String
  userId         String
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organization   Organization @relation(fields: [organizationId], references: [id])
  post           Post         @relation(fields: [postId], references: [id])
  user           User         @relation(fields: [userId], references: [id])

  @@index([createdAt])
  @@index([organizationId])
  @@index([userId])
  @@index([postId])
  @@index([deletedAt])
}

model Post {
  id                         String                    @id @default(cuid())
  state                      State                     @default(QUEUE)
  publishDate                DateTime
  organizationId             String
  integrationId              String
  content                    String
  delay                      Int                       @default(0)
  group                      String
  title                      String?
  description                String?
  parentPostId               String?
  releaseId                  String?
  releaseURL                 String?
  likesCount                 Int                       @default(0)
  likesSyncedAt              DateTime?
  settings                   String?
  image                      String?
  submittedForOrderId        String?
  submittedForOrganizationId String?
  approvedSubmitForOrder     APPROVED_SUBMIT_FOR_ORDER @default(NO)
  creationMethod             CreationMethod            @default(UNKNOWN)
  lastMessageId              String?
  intervalInDays             Int?
  error                      String?
  createdAt                  DateTime                  @default(now())
  updatedAt                  DateTime                  @updatedAt
  deletedAt                  DateTime?
  platformDeletedAt          DateTime?
  comments                   Comments[]
  errors                     Errors[]
  payoutProblems             PayoutProblems[]
  integration                Integration               @relation(fields: [integrationId], references: [id])
  lastMessage                Messages?                 @relation(fields: [lastMessageId], references: [id])
  organization               Organization              @relation("organization", fields: [organizationId], references: [id])
  parentPost                 Post?                     @relation("parentPostId", fields: [parentPostId], references: [id])
  childrenPost               Post[]                    @relation("parentPostId")
  submittedForOrder          Orders?                   @relation(fields: [submittedForOrderId], references: [id])
  submittedForOrganization   Organization?             @relation("submittedForOrg", fields: [submittedForOrganizationId], references: [id])
  tags                       TagsPosts[]
  pipelineQueueItemId        String?
  pipelineQueueItem          PipelineQueueItem?        @relation(fields: [pipelineQueueItemId], references: [id])
  httpLogs                   PostHttpLog[]
  postRuleRuns               PostRuleRun[]

  @@index([group])
  @@index([deletedAt])
  @@index([publishDate])
  @@index([state])
  @@index([organizationId])
  @@index([parentPostId])
  @@index([submittedForOrderId])
  @@index([intervalInDays])
  @@index([approvedSubmitForOrder])
  @@index([creationMethod])
  @@index([lastMessageId])
  @@index([createdAt])
  @@index([updatedAt])
  @@index([releaseURL])
  @@index([integrationId])
  @@index([pipelineQueueItemId])
  @@index([integrationId, releaseId])
}

model Notifications {
  id             String       @id @default(uuid())
  organizationId String
  content        String
  link           String?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([createdAt])
  @@index([organizationId])
  @@index([deletedAt])
}

model MessagesGroup {
  id                  String       @id @default(uuid())
  buyerOrganizationId String
  buyerId             String
  sellerId            String
  createdAt           DateTime     @default(now())
  updatedAt           DateTime     @updatedAt
  messages            Messages[]
  buyer               User         @relation("groupBuyer", fields: [buyerId], references: [id])
  buyerOrganization   Organization @relation(fields: [buyerOrganizationId], references: [id])
  seller              User         @relation("groupSeller", fields: [sellerId], references: [id])
  orders              Orders[]

  @@unique([buyerId, sellerId])
  @@index([createdAt])
  @@index([updatedAt])
  @@index([buyerOrganizationId])
}

model PayoutProblems {
  id        String   @id @default(uuid())
  status    String
  orderId   String
  userId    String
  postId    String?
  amount    Int
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  order     Orders   @relation(fields: [orderId], references: [id])
  post      Post?    @relation(fields: [postId], references: [id])
  user      User     @relation(fields: [userId], references: [id])
}

model Orders {
  id             String           @id @default(uuid())
  buyerId        String
  sellerId       String
  status         OrderStatus
  messageGroupId String
  captureId      String?
  createdAt      DateTime         @default(now())
  updatedAt      DateTime         @updatedAt
  ordersItems    OrderItems[]
  buyer          User             @relation("orderBuyer", fields: [buyerId], references: [id])
  messageGroup   MessagesGroup    @relation(fields: [messageGroupId], references: [id])
  seller         User             @relation("orderSeller", fields: [sellerId], references: [id])
  payoutProblems PayoutProblems[]
  posts          Post[]

  @@index([buyerId])
  @@index([sellerId])
  @@index([updatedAt])
  @@index([createdAt])
  @@index([messageGroupId])
}

model OrderItems {
  id            String      @id @default(uuid())
  orderId       String
  integrationId String
  quantity      Int
  price         Int
  integration   Integration @relation(fields: [integrationId], references: [id])
  order         Orders      @relation(fields: [orderId], references: [id])

  @@index([orderId])
  @@index([integrationId])
}

model Messages {
  id        String        @id @default(uuid())
  from      From
  content   String?
  groupId   String
  special   String?
  createdAt DateTime      @default(now())
  updatedAt DateTime      @updatedAt
  deletedAt DateTime?
  group     MessagesGroup @relation(fields: [groupId], references: [id])
  posts     Post[]

  @@index([groupId])
  @@index([createdAt])
  @@index([deletedAt])
}

model Plugs {
  id             String       @id @default(uuid())
  organizationId String
  plugFunction   String
  data           String
  integrationId  String
  activated      Boolean      @default(true)
  integration    Integration  @relation(fields: [integrationId], references: [id])
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@unique([plugFunction, integrationId])
  @@index([organizationId])
}

model PipelinePlug {
  id             String       @id @default(uuid())
  organizationId String
  pipelineId     String
  integrationId  String
  plugFunction   String
  data           String
  activated      Boolean      @default(true)
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id])
  pipeline       Pipeline     @relation(fields: [pipelineId], references: [id], onDelete: Cascade)
  integration    Integration  @relation(fields: [integrationId], references: [id])

  @@unique([pipelineId, integrationId, plugFunction])
  @@index([organizationId, pipelineId])
  @@index([integrationId])
}

model ExisingPlugData {
  id            String      @id @default(uuid())
  integrationId String
  methodName    String
  value         String
  integration   Integration @relation(fields: [integrationId], references: [id])

  @@unique([integrationId, methodName, value])
}

enum PostRuleAction {
  REMOVE
  AUTO_REPOST
  AUTO_PLUG
  NOTIFY
}

enum PostRuleConditionMatch {
  ANY
  ALL
}

enum PostRuleConditionMetric {
  LIKES
  REPLIES
}

enum PostRuleConditionOperator {
  LT
  LTE
  GT
  GTE
}

enum PostRuleRescheduleMode {
  MANUAL
  PIPELINE
}

enum PostRuleRunStatus {
  ACTIVE
  COMPLETED
  FAILED
  CANCELLED
}

enum PostRuleEvaluationStatus {
  PENDING
  PROCESSING
  COMPLETED
  SKIPPED
  FAILED
}

model PostRule {
  id                      String                 @id @default(cuid())
  organizationId          String
  name                    String
  enabled                 Boolean                @default(true)
  action                  PostRuleAction
  initialDelayHours       Int
  evaluationIntervalHours Int?
  maxEvaluations          Int?
  conditionMatch          PostRuleConditionMatch @default(ANY)
  conditions              Json                   @default("[]")
  actionConfig            Json                   @default("{}")
  rescheduleConfig        Json?
  maxRescheduleAttempts   Int?
  createdAt               DateTime               @default(now())
  updatedAt               DateTime               @updatedAt
  organization            Organization           @relation(fields: [organizationId], references: [id])
  integrations            PostRuleIntegration[]
  pipelines               PostRulePipeline[]
  runs                    PostRuleRun[]

  @@index([organizationId, enabled])
  @@index([organizationId, action])
  @@index([organizationId])
}

model PostRuleIntegration {
  id             String       @id @default(cuid())
  organizationId String
  ruleId         String
  integrationId  String
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id])
  rule           PostRule     @relation(fields: [ruleId], references: [id], onDelete: Cascade)
  integration    Integration  @relation(fields: [integrationId], references: [id])

  @@unique([ruleId, integrationId])
  @@index([organizationId, integrationId])
  @@index([integrationId])
  @@index([ruleId])
}

model PostRulePipeline {
  id             String       @id @default(cuid())
  organizationId String
  ruleId         String
  pipelineId     String
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id])
  rule           PostRule     @relation(fields: [ruleId], references: [id], onDelete: Cascade)
  pipeline       Pipeline     @relation(fields: [pipelineId], references: [id])

  @@unique([ruleId, pipelineId])
  @@index([organizationId, pipelineId])
  @@index([pipelineId])
  @@index([ruleId])
}

model PostRuleRun {
  id                String               @id @default(cuid())
  organizationId    String
  ruleId            String?
  postId            String
  lineageId         String
  rescheduleAttempt Int                  @default(0)
  status            PostRuleRunStatus    @default(ACTIVE)
  completedAt       DateTime?
  failedAt          DateTime?
  createdAt         DateTime             @default(now())
  updatedAt         DateTime             @updatedAt
  organization      Organization         @relation(fields: [organizationId], references: [id])
  rule              PostRule?            @relation(fields: [ruleId], references: [id], onDelete: SetNull)
  post              Post                 @relation(fields: [postId], references: [id])
  evaluations       PostRuleEvaluation[]

  @@unique([ruleId, postId])
  @@index([organizationId])
  @@index([postId])
  @@index([lineageId])
  @@index([status])
}

model PostRuleEvaluation {
  id              String                   @id @default(cuid())
  organizationId  String
  runId           String
  evaluationIndex Int
  scheduledAt     DateTime
  status          PostRuleEvaluationStatus @default(PENDING)
  claimedAt       DateTime?
  completedAt     DateTime?
  metrics         Json?
  actionResult    Json?
  errorSummary    String?
  createdAt       DateTime                 @default(now())
  updatedAt       DateTime                 @updatedAt
  organization    Organization             @relation(fields: [organizationId], references: [id])
  run             PostRuleRun              @relation(fields: [runId], references: [id], onDelete: Cascade)

  @@unique([runId, evaluationIndex])
  @@index([runId, status])
  @@index([scheduledAt])
  @@index([organizationId])
}

model PopularPosts {
  id        String   @id @default(uuid())
  category  String
  topic     String
  content   String
  hook      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model IntegrationsWebhooks {
  integrationId String
  webhookId     String
  integration   Integration @relation(fields: [integrationId], references: [id])
  webhook       Webhooks    @relation(fields: [webhookId], references: [id])

  @@id([integrationId, webhookId])
  @@unique([integrationId, webhookId])
  @@index([integrationId])
  @@index([webhookId])
}

model Webhooks {
  id             String                 @id @default(uuid())
  name           String
  organizationId String
  url            String
  deletedAt      DateTime?
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt
  integrations   IntegrationsWebhooks[]
  organization   Organization           @relation(fields: [organizationId], references: [id])
  httpLogs       WebhookHttpLog[]

  @@index([organizationId])
  @@index([deletedAt])
}

model AutoPost {
  id              String       @id @default(uuid())
  organizationId  String
  pipelineId      String?
  title           String
  content         String?
  onSlot          Boolean
  syncLast        Boolean
  url             String
  lastUrl         String
  active          Boolean
  addPicture      Boolean
  generateContent Boolean
  integrations    String
  deletedAt       DateTime?
  createdAt       DateTime     @default(now())
  updatedAt       DateTime     @updatedAt
  organization    Organization @relation(fields: [organizationId], references: [id])
  pipeline        Pipeline?    @relation(fields: [pipelineId], references: [id])

  @@index([deletedAt])
  @@index([organizationId, pipelineId, deletedAt])
  @@index([pipelineId])
}

model Pipeline {
  id                String                    @id @default(uuid())
  organizationId    String
  name              String
  timezone          String
  color             String                    @default("#612BD3")
  active            Boolean                   @default(true)
  scheduleRevision  Int                       @default(1)
  deletedAt         DateTime?
  createdAt         DateTime                  @default(now())
  updatedAt         DateTime                  @updatedAt
  organization      Organization              @relation(fields: [organizationId], references: [id])
  integrations      PipelineIntegration[]
  scheduleSlots     PipelineScheduleSlot[]
  queueItems        PipelineQueueItem[]
  slotExecutions    PipelineSlotExecution[]
  contextDocuments  PipelineContextDocument[]
  plugs             PipelinePlug[]
  autoposts         AutoPost[]
  postRulePipelines PostRulePipeline[]

  @@index([organizationId, deletedAt])
  @@index([organizationId, active])
  @@index([organizationId, scheduleRevision])
  @@index([deletedAt])
}

model PipelineIntegration {
  id            String      @id @default(uuid())
  pipelineId    String
  integrationId String
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt
  pipeline      Pipeline    @relation(fields: [pipelineId], references: [id], onDelete: Cascade)
  integration   Integration @relation(fields: [integrationId], references: [id])

  @@unique([pipelineId, integrationId])
  @@index([integrationId, pipelineId])
  @@index([pipelineId])
}

model PipelineScheduleSlot {
  id          String   @id @default(uuid())
  pipelineId  String
  dayOfWeek   Int
  minuteOfDay Int
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  pipeline    Pipeline @relation(fields: [pipelineId], references: [id], onDelete: Cascade)

  @@unique([pipelineId, dayOfWeek, minuteOfDay])
  @@index([pipelineId, dayOfWeek, minuteOfDay])
}

model PipelineQueueItem {
  id             String                  @id @default(uuid())
  pipelineId     String
  group          String                  @unique
  idempotencyKey String?                 @unique
  position       Int
  status         PipelineQueueItemStatus @default(QUEUED)
  claimedAt      DateTime?
  publishedAt    DateTime?
  failedAt       DateTime?
  deletedAt      DateTime?
  error          String?
  createdAt      DateTime                @default(now())
  updatedAt      DateTime                @updatedAt
  pipeline       Pipeline                @relation(fields: [pipelineId], references: [id])
  posts          Post[]
  slotExecutions PipelineSlotExecution[]

  @@index([pipelineId, status, position])
  @@index([pipelineId, status, position, createdAt, id])
  @@index([claimedAt])
  @@index([failedAt])
  @@index([deletedAt])
}

model PipelineSlotExecution {
  id                  String                      @id @default(uuid())
  pipelineId          String
  pipelineQueueItemId String?
  scheduledFor        DateTime
  scheduleRevision    Int
  status              PipelineSlotExecutionStatus @default(SKIPPED)
  claimedAt           DateTime?
  completedAt         DateTime?
  failedAt            DateTime?
  error               String?
  createdAt           DateTime                    @default(now())
  updatedAt           DateTime                    @updatedAt
  pipeline            Pipeline                    @relation(fields: [pipelineId], references: [id])
  pipelineQueueItem   PipelineQueueItem?          @relation(fields: [pipelineQueueItemId], references: [id])

  @@unique([pipelineId, scheduledFor])
  @@index([pipelineId, status, scheduledFor])
  @@index([pipelineQueueItemId])
  @@index([claimedAt])
  @@index([failedAt])
}

model ContextDocument {
  id                     String                       @id @default(uuid())
  organizationId         String
  name                   String
  description            String?                      @db.VarChar(500)
  content                String
  fileSize               Int
  createdAt              DateTime                     @default(now())
  updatedAt              DateTime                     @updatedAt
  organization           Organization                 @relation(fields: [organizationId], references: [id])
  pipelineAssignments    PipelineContextDocument[]
  integrationAssignments IntegrationContextDocument[]

  @@unique([organizationId, name])
  @@index([organizationId])
}

model PipelineContextDocument {
  pipelineId        String
  contextDocumentId String
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt
  pipeline          Pipeline        @relation(fields: [pipelineId], references: [id], onDelete: Cascade)
  contextDocument   ContextDocument @relation(fields: [contextDocumentId], references: [id], onDelete: Cascade)

  @@id([pipelineId, contextDocumentId])
  @@index([contextDocumentId, pipelineId])
}

model IntegrationContextDocument {
  integrationId     String
  contextDocumentId String
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt
  integration       Integration     @relation(fields: [integrationId], references: [id], onDelete: Cascade)
  contextDocument   ContextDocument @relation(fields: [contextDocumentId], references: [id], onDelete: Cascade)

  @@id([integrationId, contextDocumentId])
  @@index([contextDocumentId, integrationId])
}

model Sets {
  id             String       @id @default(uuid())
  organizationId String
  name           String
  content        String
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId])
}

model ThirdParty {
  id             String       @id @default(uuid())
  organizationId String
  identifier     String
  name           String
  internalId     String
  apiKey         String
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organization   Organization @relation(fields: [organizationId], references: [id])

  @@unique([organizationId, internalId])
  @@index([organizationId])
  @@index([deletedAt])
}

model Errors {
  id             String       @id @default(uuid())
  message        String
  platform       String
  organizationId String
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  postId         String
  body           String       @default("{}")
  organization   Organization @relation(fields: [organizationId], references: [id])
  post           Post         @relation(fields: [postId], references: [id])

  @@index([organizationId])
  @@index([createdAt])
}

model Mentions {
  name      String
  username  String
  platform  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  image     String

  @@id([name, username, platform, image])
  @@index([createdAt])
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model mastra_ai_spans {
  traceId      String
  spanId       String
  parentSpanId String?
  name         String
  scope        Json?
  spanType     String
  attributes   Json?
  metadata     Json?
  links        Json?
  input        Json?
  output       Json?
  error        Json?
  startedAt    DateTime  @db.Timestamp(6)
  endedAt      DateTime? @db.Timestamp(6)
  createdAt    DateTime  @db.Timestamp(6)
  updatedAt    DateTime? @db.Timestamp(6)
  isEvent      Boolean
  startedAtZ   DateTime? @default(now()) @db.Timestamptz(6)
  endedAtZ     DateTime? @default(now()) @db.Timestamptz(6)
  createdAtZ   DateTime? @default(now()) @db.Timestamptz(6)
  updatedAtZ   DateTime? @default(now()) @db.Timestamptz(6)

  @@index([name], map: "public_mastra_ai_spans_name_idx")
  @@index([parentSpanId, startedAt(sort: Desc)], map: "public_mastra_ai_spans_parentspanid_startedat_idx")
  @@index([spanType, startedAt(sort: Desc)], map: "public_mastra_ai_spans_spantype_startedat_idx")
  @@index([traceId, startedAt(sort: Desc)], map: "public_mastra_ai_spans_traceid_startedat_idx")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model mastra_evals {
  input         String
  output        String
  result        Json
  agent_name    String
  metric_name   String
  instructions  String
  test_info     Json?
  global_run_id String
  run_id        String
  created_at    DateTime  @db.Timestamp(6)
  createdAt     DateTime? @db.Timestamp(6)
  created_atZ   DateTime? @default(now()) @db.Timestamptz(6)
  createdAtZ    DateTime? @default(now()) @db.Timestamptz(6)

  @@index([agent_name, created_at(sort: Desc)], map: "public_mastra_evals_agent_name_created_at_idx")
  @@ignore
}

model mastra_messages {
  id         String    @id
  thread_id  String
  content    String
  role       String
  type       String
  createdAt  DateTime  @db.Timestamp(6)
  resourceId String?
  createdAtZ DateTime? @default(now()) @db.Timestamptz(6)

  @@index([thread_id, createdAt(sort: Desc)], map: "public_mastra_messages_thread_id_createdat_idx")
}

model mastra_resources {
  id            String    @id
  workingMemory String?
  metadata      Json?
  createdAt     DateTime  @db.Timestamp(6)
  updatedAt     DateTime  @db.Timestamp(6)
  createdAtZ    DateTime? @default(now()) @db.Timestamptz(6)
  updatedAtZ    DateTime? @default(now()) @db.Timestamptz(6)
}

model mastra_scorers {
  id                   String    @id
  scorerId             String
  traceId              String?
  runId                String
  scorer               Json
  preprocessStepResult Json?
  extractStepResult    Json?
  analyzeStepResult    Json?
  score                Float
  reason               String?
  metadata             Json?
  preprocessPrompt     String?
  extractPrompt        String?
  generateScorePrompt  String?
  generateReasonPrompt String?
  analyzePrompt        String?
  reasonPrompt         String?
  input                Json
  output               Json
  additionalContext    Json?
  runtimeContext       Json?
  entityType           String?
  entity               Json?
  entityId             String?
  source               String
  resourceId           String?
  threadId             String?
  createdAt            DateTime  @db.Timestamp(6)
  updatedAt            DateTime  @db.Timestamp(6)
  createdAtZ           DateTime? @default(now()) @db.Timestamptz(6)
  updatedAtZ           DateTime? @default(now()) @db.Timestamptz(6)
  spanId               String?

  @@index([traceId, spanId, createdAt(sort: Desc)], map: "public_mastra_scores_trace_id_span_id_created_at_idx")
}

model mastra_threads {
  id         String    @id
  resourceId String
  title      String
  metadata   String?
  createdAt  DateTime  @db.Timestamp(6)
  updatedAt  DateTime  @db.Timestamp(6)
  createdAtZ DateTime? @default(now()) @db.Timestamptz(6)
  updatedAtZ DateTime? @default(now()) @db.Timestamptz(6)

  @@index([resourceId, createdAt(sort: Desc)], map: "public_mastra_threads_resourceid_createdat_idx")
}

model mastra_traces {
  id           String    @id
  parentSpanId String?
  name         String
  traceId      String
  scope        String
  kind         Int
  attributes   Json?
  status       Json?
  events       Json?
  links        Json?
  other        String?
  startTime    BigInt
  endTime      BigInt
  createdAt    DateTime  @db.Timestamp(6)
  createdAtZ   DateTime? @default(now()) @db.Timestamptz(6)

  @@index([name, startTime(sort: Desc)], map: "public_mastra_traces_name_starttime_idx")
}

model mastra_workflow_snapshot {
  workflow_name String
  run_id        String
  resourceId    String?
  snapshot      String
  createdAt     DateTime  @db.Timestamp(6)
  updatedAt     DateTime  @db.Timestamp(6)
  createdAtZ    DateTime? @default(now()) @db.Timestamptz(6)
  updatedAtZ    DateTime? @default(now()) @db.Timestamptz(6)

  @@unique([workflow_name, run_id], map: "public_mastra_workflow_snapshot_workflow_name_run_id_key")
}

model OAuthApp {
  id             String               @id @default(uuid())
  organizationId String
  name           String
  description    String?
  pictureId      String?
  redirectUrl    String
  clientId       String               @unique
  clientSecret   String
  createdAt      DateTime             @default(now())
  updatedAt      DateTime             @updatedAt
  deletedAt      DateTime?
  organization   Organization         @relation(fields: [organizationId], references: [id])
  picture        Media?               @relation(fields: [pictureId], references: [id])
  authorizations OAuthAuthorization[]

  @@unique([organizationId, deletedAt])
  @@index([clientId])
  @@index([organizationId])
  @@index([deletedAt])
}

model OAuthAuthorization {
  id                String       @id @default(uuid())
  oauthAppId        String
  userId            String
  organizationId    String
  accessToken       String?
  authorizationCode String?
  codeExpiresAt     DateTime?
  revokedAt         DateTime?
  createdAt         DateTime     @default(now())
  updatedAt         DateTime     @updatedAt
  oauthApp          OAuthApp     @relation(fields: [oauthAppId], references: [id])
  user              User         @relation(fields: [userId], references: [id])
  organization      Organization @relation(fields: [organizationId], references: [id])

  @@unique([oauthAppId, userId, organizationId])
  @@index([accessToken])
  @@index([authorizationCode])
  @@index([oauthAppId])
  @@index([userId])
  @@index([organizationId])
  @@index([revokedAt])
}

enum OrderStatus {
  PENDING
  ACCEPTED
  CANCELED
  COMPLETED
}

enum From {
  BUYER
  SELLER
}

enum State {
  QUEUE
  PUBLISHED
  ERROR
  DRAFT
}

enum PipelineQueueItemStatus {
  CREATING
  QUEUED
  PUBLISHING
  PUBLISHED
  FAILED
  REMOVED
}

enum PipelineSlotExecutionStatus {
  SKIPPED
  CLAIMED
  COMPLETED
  FAILED
}

enum SubscriptionTier {
  STANDARD
  PRO
  TEAM
  ULTIMATE
}

enum Period {
  MONTHLY
  YEARLY
}

enum Provider {
  LOCAL
  GITHUB
  GOOGLE
  FARCASTER
  WALLET
  GENERIC
}

enum Role {
  SUPERADMIN
  ADMIN
  USER
}

enum APPROVED_SUBMIT_FOR_ORDER {
  NO
  WAITING_CONFIRMATION
  YES
}

enum CreationMethod {
  UNKNOWN
  WEB
  MCP
  API
  AUTOPOST
  CLI
  PLATFORM
}

enum ShortLinkPreference {
  ASK
  YES
  NO
}

enum AnnouncementColor {
  INFO
  WARNING
  ERROR
}

model Announcement {
  id          String            @id @default(uuid())
  title       String
  description String
  color       AnnouncementColor @default(INFO)
  createdAt   DateTime          @default(now())
}

model ChannelAudienceMember {
  id                             String                              @id @default(cuid())
  organizationId                 String
  integrationId                  String
  externalId                     String
  name                           String?
  username                       String?
  picture                        String?
  profileUrl                     String?
  bio                            String?
  followersCount                 Int?
  followingCount                 Int?
  membershipState                ChannelAudienceMembership           @default(UNKNOWN)
  followedAt                     DateTime?
  weFollowedAt                   DateTime?
  accountCreatedAt               DateTime?
  followerSyncGeneration         String?
  membershipEvidenceGeneration   String?
  noteCount                      Int                                 @default(0)
  likesCount                     Int                                 @default(0)
  inboundInteractionCount        Int                                 @default(0)
  lastInboundAt                  DateTime?
  lastOutboundAt                 DateTime?
  leadBridgeScore                Float?
  leadFitScore                   Float?
  leadFitReason                  String?
  leadFitConcerns                String?
  leadFitMatchedTopics           String?
  leadFitModel                   String?
  leadFitVersion                 Int?
  leadFitScoredAt                DateTime?
  relationshipGrade              Float?
  relationshipEffortScore        Int?
  relationshipReciprocationScore Int?
  relationshipNetGap             Int?
  relationshipTriage             String?
  relationshipFormulaVersion     Int?
  relationshipStrategyId         String?
  relationshipStrategyVersion    Int?
  relationshipSnapshotAt         DateTime?
  botGrade                       Int?
  isBot                          Boolean?
  botConfidence                  Float?
  botFormulaVersion              Int?
  botGradedAt                    DateTime?
  ignoredAt                      DateTime?
  ignoredByUserId                String?
  createdAt                      DateTime                            @default(now())
  updatedAt                      DateTime                            @updatedAt
  organization                   Organization                        @relation(fields: [organizationId], references: [id])
  integration                    Integration                         @relation(fields: [integrationId], references: [id])
  ignoredBy                      User?                               @relation("AudienceMemberIgnoredBy", fields: [ignoredByUserId], references: [id])
  interactionSummaries           ChannelInteractionWindowSummary[]   @relation("AudienceMemberInteractionSummaries")
  notes                          ChannelAudienceNote[]               @relation("AudienceMemberNotes")
  listMemberships                ChannelAudienceListMember[]         @relation("AudienceMemberListMemberships")
  personalGrades                 ChannelAudienceMemberGrade[]        @relation("AudienceMemberPersonalGrades")
  gradeSnapshots                 ChannelRelationshipGradeSnapshot[]  @relation("AudienceMemberGradeSnapshots")
  triageIgnores                  ChannelAudienceMemberTriageIgnore[] @relation("AudienceMemberTriageIgnores")
  leadFitFeedbacks               ChannelAudienceLeadFitFeedback[]    @relation("AudienceMemberLeadFitFeedbacks")
  leadBridgesAsLead              ChannelAudienceLeadBridge[]         @relation("AudienceLeadBridgesAsLead")
  leadBridgesAsBridge            ChannelAudienceLeadBridge[]         @relation("AudienceLeadBridgesAsBridge")
  cultivatePicks                 ChannelAudienceCultivatePick[]      @relation("AudienceMemberCultivatePicks")
  hotPicks                       ChannelAudienceHotPick[]            @relation("AudienceMemberHotPicks")

  @@unique([integrationId, externalId])
  @@index([organizationId, integrationId, membershipState])
  @@index([integrationId, followerSyncGeneration])
  @@index([integrationId, membershipEvidenceGeneration])
  @@index([integrationId, membershipState, noteCount, externalId])
  @@index([integrationId, membershipState, likesCount, externalId])
  @@index([integrationId, membershipState, lastInboundAt, externalId], map: "ChannelAudienceMember_lead_inbound_keyset_idx")
  @@index([integrationId, membershipState, leadBridgeScore, lastInboundAt, externalId], map: "ChannelAudienceMember_lead_bridge_keyset_idx")
  @@index([integrationId, membershipState, leadFitScore, leadBridgeScore, lastInboundAt, externalId], map: "ChannelAudienceMember_lead_fit_keyset_idx")
  @@index([integrationId, membershipState, relationshipGrade, externalId])
  @@index([integrationId, membershipState, relationshipEffortScore, externalId], map: "ChannelAudienceMember_rel_effort_keyset_idx")
  @@index([integrationId, membershipState, relationshipReciprocationScore, externalId], map: "ChannelAudienceMember_rel_reciprocation_keyset_idx")
  @@index([integrationId, membershipState, relationshipNetGap, externalId], map: "ChannelAudienceMember_rel_net_gap_keyset_idx")
  @@index([integrationId, membershipState, relationshipTriage, externalId], map: "ChannelAudienceMember_rel_triage_keyset_idx")
  @@index([integrationId, membershipState, relationshipFormulaVersion, relationshipStrategyId, relationshipStrategyVersion, relationshipSnapshotAt], map: "ChannelAudienceMember_rel_formula_due_idx")
  @@index([integrationId, membershipState, botGrade, externalId], map: "ChannelAudienceMember_bot_grade_keyset_idx")
  @@index([integrationId, membershipState, isBot, externalId], map: "ChannelAudienceMember_is_bot_keyset_idx")
  @@index([integrationId, membershipState, botFormulaVersion, botGradedAt], map: "ChannelAudienceMember_bot_formula_due_idx")
  @@index([integrationId, membershipState, ignoredAt, externalId], map: "ChannelAudienceMember_ignored_keyset_idx")
  @@index([integrationId, membershipState, lastOutboundAt, externalId], map: "ChannelAudienceMember_last_outbound_keyset_idx")
  @@index([integrationId, membershipState, weFollowedAt, externalId], map: "ChannelAudienceMember_followed_keyset_idx")
  @@index([ignoredByUserId])
  @@index([integrationId])
  @@index([organizationId])
}

model ChannelInteractionEvent {
  id                     String                      @id @default(cuid())
  organizationId         String
  integrationId          String
  providerEventKey       String
  counterpartyExternalId String
  kind                   ChannelInteractionKind
  direction              ChannelInteractionDirection
  eventAt                DateTime
  relatedObjectId        String?
  metadata               Json?
  normalizationVersion   Int                         @default(1)
  createdAt              DateTime                    @default(now())
  organization           Organization                @relation(fields: [organizationId], references: [id])
  integration            Integration                 @relation(fields: [integrationId], references: [id])

  @@unique([integrationId, providerEventKey])
  @@index([integrationId, counterpartyExternalId, eventAt])
  @@index([integrationId, kind, direction, eventAt])
  @@index([organizationId])
}

model ChannelInteractionDailyAggregate {
  id                     String       @id @default(cuid())
  organizationId         String
  integrationId          String
  counterpartyExternalId String
  day                    DateTime     @db.Date
  interactionCount       Int          @default(0)
  interactionScore       Int          @default(0)
  lastInteractionAt      DateTime?
  createdAt              DateTime     @default(now())
  updatedAt              DateTime     @updatedAt
  organization           Organization @relation(fields: [organizationId], references: [id])
  integration            Integration  @relation(fields: [integrationId], references: [id])

  @@unique([integrationId, counterpartyExternalId, day])
  @@index([integrationId, day])
  @@index([organizationId])
}

model ChannelInteractionWindowSummary {
  id                     String                   @id @default(cuid())
  organizationId         String
  integrationId          String
  window                 ChannelInteractionWindow
  generation             String
  counterpartyExternalId String
  interactionCount       Int                      @default(0)
  interactionScore       Int                      @default(0)
  lastInteractionAt      DateTime?
  computedAt             DateTime
  createdAt              DateTime                 @default(now())
  organization           Organization             @relation(fields: [organizationId], references: [id])
  integration            Integration              @relation(fields: [integrationId], references: [id])
  audienceMember         ChannelAudienceMember    @relation("AudienceMemberInteractionSummaries", fields: [integrationId, counterpartyExternalId], references: [integrationId, externalId])

  @@unique([integrationId, window, generation, counterpartyExternalId])
  @@index([integrationId, window, generation, interactionCount, interactionScore, lastInteractionAt, counterpartyExternalId])
  @@index([organizationId])
}

model ChannelInteractionRollupState {
  id               String                   @id @default(cuid())
  organizationId   String
  integrationId    String
  window           ChannelInteractionWindow
  activeGeneration String
  computedAt       DateTime
  createdAt        DateTime                 @default(now())
  updatedAt        DateTime                 @updatedAt
  organization     Organization             @relation(fields: [organizationId], references: [id])
  integration      Integration              @relation(fields: [integrationId], references: [id])

  @@unique([integrationId, window])
  @@index([organizationId])
}

model ChannelFollowerSyncState {
  id                String                    @id @default(cuid())
  organizationId    String
  integrationId     String                    @unique
  activeGeneration  String?
  pendingGeneration String?
  status            ChannelFollowerSyncStatus @default(IN_PROGRESS)
  completedAt       DateTime?
  createdAt         DateTime                  @default(now())
  updatedAt         DateTime                  @updatedAt
  organization      Organization              @relation(fields: [organizationId], references: [id])
  integration       Integration               @relation(fields: [integrationId], references: [id])

  @@index([organizationId])
  @@index([integrationId, pendingGeneration, status])
}

model ChannelInteractionSubscription {
  id                String                          @id @default(cuid())
  organizationId    String
  integrationId     String
  eventKey          String
  direction         ChannelInteractionDirection
  remoteIdentifier  String?
  state             ChannelInteractionTrackingState @default(UNCONFIGURED)
  trackingStartedAt DateTime?
  failureCategory   String?
  failureReason     String?
  createdAt         DateTime                        @default(now())
  updatedAt         DateTime                        @updatedAt
  organization      Organization                    @relation(fields: [organizationId], references: [id])
  integration       Integration                     @relation(fields: [integrationId], references: [id])

  @@unique([integrationId, eventKey, direction])
  @@index([integrationId, state])
  @@index([organizationId])
}

model ChannelInteractionAuthorization {
  id              String       @id @default(cuid())
  organizationId  String
  integrationId   String       @unique
  token           String
  refreshToken    String?
  tokenExpiration DateTime?
  scopes          String?
  createdAt       DateTime     @default(now())
  updatedAt       DateTime     @updatedAt
  organization    Organization @relation(fields: [organizationId], references: [id])
  integration     Integration  @relation(fields: [integrationId], references: [id])

  @@index([organizationId])
}

model ProviderWebhookEndpoint {
  id                 String                          @id @default(cuid())
  providerIdentifier String
  callbackIdentity   String
  remoteWebhookId    String?
  state              ChannelInteractionTrackingState @default(UNCONFIGURED)
  lastError          String?
  createdAt          DateTime                        @default(now())
  updatedAt          DateTime                        @updatedAt

  @@unique([providerIdentifier, callbackIdentity])
  @@index([providerIdentifier, state])
}

enum ChannelInteractionKind {
  LIKE
  REPLY
  REPOST
  FOLLOW
  MENTION
}

enum ChannelInteractionDirection {
  INBOUND
  OUTBOUND
}

enum ChannelAudienceMembership {
  FOLLOWER
  NOT_FOLLOWER
  UNKNOWN
}

enum ChannelInteractionWindow {
  WEEK
  MONTH
  NINETY_DAY
  YEAR
}

enum ChannelInteractionTrackingState {
  UNCONFIGURED
  REMOVING
  PROVISIONING
  ACTIVE
  PARTIAL
  ERROR
}

enum ChannelFollowerSyncStatus {
  IN_PROGRESS
  COMPLETE
  FAILED
}

enum ChannelAnalyticsValueMode {
  SUM
  AVERAGE
  LATEST
}

enum ChannelAnalyticsDisplayUnit {
  COUNT
  PERCENTAGE
  DURATION
  DECIMAL
}

model ChannelAnalyticsDailyPoint {
  id                 String                       @id @default(cuid())
  organizationId     String
  integrationId      String
  day                DateTime                     @db.Date
  metricKey          String
  label              String
  valueMode          ChannelAnalyticsValueMode
  displayUnit        ChannelAnalyticsDisplayUnit?
  value              Decimal                      @db.Decimal(30, 6)
  currentSnapshotAt  DateTime?
  previousSnapshotAt DateTime?
  createdAt          DateTime                     @default(now())
  updatedAt          DateTime                     @updatedAt
  organization       Organization                 @relation(fields: [organizationId], references: [id])
  integration        Integration                  @relation(fields: [integrationId], references: [id])

  @@unique([integrationId, day, metricKey])
  @@index([organizationId, integrationId, day])
  @@index([integrationId, day])
}

model ChannelAnalyticsPostMetricSnapshot {
  id             String                       @id @default(cuid())
  organizationId String
  integrationId  String
  snapshotAt     DateTime
  externalPostId String
  metricKey      String
  label          String
  valueMode      ChannelAnalyticsValueMode
  displayUnit    ChannelAnalyticsDisplayUnit?
  value          Decimal                      @db.Decimal(30, 6)
  createdAt      DateTime                     @default(now())
  organization   Organization                 @relation(fields: [organizationId], references: [id])
  integration    Integration                  @relation(fields: [integrationId], references: [id])

  @@unique([integrationId, snapshotAt, externalPostId, metricKey])
  @@index([organizationId, integrationId, snapshotAt])
  @@index([integrationId, snapshotAt])
}

model ChannelAnalyticsSyncState {
  id                        String       @id @default(cuid())
  organizationId            String
  integrationId             String       @unique
  lastSuccessfulSnapshotAt  DateTime?
  lastCoveredDay            DateTime?    @db.Date
  coverageStartDay          DateTime?    @db.Date
  coverageEndDay            DateTime?    @db.Date
  pendingCoverageSnapshotAt DateTime?
  pendingCoverageStartDay   DateTime?    @db.Date
  pendingCoverageEndDay     DateTime?    @db.Date
  nextAttemptAt             DateTime?
  failureCount              Int          @default(0)
  failureCategory           String?
  failureMessage            String?
  createdAt                 DateTime     @default(now())
  updatedAt                 DateTime     @updatedAt
  organization              Organization @relation(fields: [organizationId], references: [id])
  integration               Integration  @relation(fields: [integrationId], references: [id])

  @@index([nextAttemptAt, integrationId])
  @@index([organizationId])
}

model ChannelAudienceMemberGrade {
  id                     String                @id @default(cuid())
  organizationId         String
  integrationId          String
  counterpartyExternalId String
  userId                 String
  grade                  Float
  createdAt              DateTime              @default(now())
  updatedAt              DateTime              @updatedAt
  organization           Organization          @relation(fields: [organizationId], references: [id])
  integration            Integration           @relation(fields: [integrationId], references: [id])
  user                   User                  @relation(fields: [userId], references: [id])
  audienceMember         ChannelAudienceMember @relation("AudienceMemberPersonalGrades", fields: [integrationId, counterpartyExternalId], references: [integrationId, externalId])

  @@unique([organizationId, integrationId, counterpartyExternalId, userId])
  @@index([organizationId, integrationId, counterpartyExternalId])
  @@index([userId])
  @@index([organizationId])
}

model ChannelAudienceList {
  id              String                      @id @default(cuid())
  organizationId  String
  integrationId   String
  name            String
  createdByUserId String?
  deletedAt       DateTime?
  createdAt       DateTime                    @default(now())
  updatedAt       DateTime                    @updatedAt
  organization    Organization                @relation(fields: [organizationId], references: [id])
  integration     Integration                 @relation(fields: [integrationId], references: [id])
  createdBy       User?                       @relation(fields: [createdByUserId], references: [id])
  members         ChannelAudienceListMember[]

  @@index([organizationId, integrationId, deletedAt])
  @@index([integrationId, deletedAt])
  @@index([createdByUserId])
  @@index([organizationId])
}

model ChannelAudienceListMember {
  organizationId         String
  integrationId          String
  listId                 String
  counterpartyExternalId String
  createdAt              DateTime              @default(now())
  organization           Organization          @relation(fields: [organizationId], references: [id])
  integration            Integration           @relation(fields: [integrationId], references: [id])
  list                   ChannelAudienceList   @relation(fields: [listId], references: [id])
  audienceMember         ChannelAudienceMember @relation("AudienceMemberListMemberships", fields: [integrationId, counterpartyExternalId], references: [integrationId, externalId])

  @@id([listId, counterpartyExternalId])
  @@index([organizationId, integrationId, listId])
  @@index([integrationId, counterpartyExternalId])
  @@index([organizationId])
}

model ChannelAudienceMemberTriageIgnore {
  organizationId         String
  integrationId          String
  counterpartyExternalId String
  triage                 String
  createdAt              DateTime              @default(now())
  expiresAt              DateTime?
  createdByUserId        String?
  organization           Organization          @relation(fields: [organizationId], references: [id])
  integration            Integration           @relation(fields: [integrationId], references: [id])
  createdBy              User?                 @relation(fields: [createdByUserId], references: [id])
  audienceMember         ChannelAudienceMember @relation("AudienceMemberTriageIgnores", fields: [integrationId, counterpartyExternalId], references: [integrationId, externalId])

  @@id([organizationId, integrationId, counterpartyExternalId, triage])
  @@index([organizationId, integrationId, triage])
  @@index([integrationId, counterpartyExternalId])
  @@index([createdByUserId])
  @@index([organizationId])
  @@index([expiresAt])
}

model ChannelAudienceLeadFitFeedback {
  id                     String                @id @default(cuid())
  organizationId         String
  integrationId          String
  counterpartyExternalId String
  source                 String
  verdict                String
  reasons                String?
  name                   String?
  username               String?
  bio                    String?
  followersCount         Int?
  followingCount         Int?
  leadFitScore           Float?
  leadFitReason          String?
  leadFitMatchedTopics   String?
  listId                 String?
  createdByUserId        String?
  createdAt              DateTime              @default(now())
  updatedAt              DateTime              @updatedAt
  organization           Organization          @relation(fields: [organizationId], references: [id])
  integration            Integration           @relation(fields: [integrationId], references: [id])
  createdBy              User?                 @relation(fields: [createdByUserId], references: [id])
  audienceMember         ChannelAudienceMember @relation("AudienceMemberLeadFitFeedbacks", fields: [integrationId, counterpartyExternalId], references: [integrationId, externalId])

  @@unique([organizationId, integrationId, counterpartyExternalId, source])
  @@index([organizationId, integrationId, verdict, updatedAt])
  @@index([integrationId, counterpartyExternalId])
  @@index([createdByUserId])
  @@index([organizationId])
}

model ChannelAudienceLeadBridge {
  organizationId          String
  integrationId           String
  leadExternalId          String
  bridgeExternalId        String
  bridgeRelationshipGrade Float?
  discoveredAt            DateTime              @default(now())
  lastSeenAt              DateTime              @default(now())
  organization            Organization          @relation(fields: [organizationId], references: [id])
  integration             Integration           @relation(fields: [integrationId], references: [id])
  leadMember              ChannelAudienceMember @relation("AudienceLeadBridgesAsLead", fields: [integrationId, leadExternalId], references: [integrationId, externalId])
  bridgeMember            ChannelAudienceMember @relation("AudienceLeadBridgesAsBridge", fields: [integrationId, bridgeExternalId], references: [integrationId, externalId])

  @@id([organizationId, integrationId, leadExternalId, bridgeExternalId])
  @@index([integrationId, leadExternalId])
  @@index([integrationId, bridgeExternalId])
  @@index([organizationId])
}

model ChannelAudienceCultivatePickBatch {
  organizationId         String
  integrationId          String
  hour                   String
  strategyId             String
  strategyVersion        Int
  materializationVersion Int
  candidateCount         Int
  pickCount              Int
  source                 String                         @default("rules")
  completedAt            DateTime
  createdAt              DateTime                       @default(now())
  updatedAt              DateTime                       @updatedAt
  organization           Organization                   @relation(fields: [organizationId], references: [id])
  integration            Integration                    @relation(fields: [integrationId], references: [id])
  picks                  ChannelAudienceCultivatePick[]

  @@id([organizationId, integrationId, hour])
  @@index([integrationId, hour])
  @@index([organizationId])
}

model ChannelAudienceCultivatePick {
  organizationId         String
  integrationId          String
  hour                   String
  counterpartyExternalId String
  rulesRank              Int
  finalRank              Int
  rulesReason            String
  aiRank                 Int?
  aiReason               String?
  suggestedAction        String?
  source                 String                            @default("rules")
  createdAt              DateTime                          @default(now())
  updatedAt              DateTime                          @updatedAt
  organization           Organization                      @relation(fields: [organizationId], references: [id])
  integration            Integration                       @relation(fields: [integrationId], references: [id])
  batch                  ChannelAudienceCultivatePickBatch @relation(fields: [organizationId, integrationId, hour], references: [organizationId, integrationId, hour])
  audienceMember         ChannelAudienceMember             @relation("AudienceMemberCultivatePicks", fields: [integrationId, counterpartyExternalId], references: [integrationId, externalId])

  @@id([organizationId, integrationId, hour, counterpartyExternalId])
  @@index([integrationId, hour, finalRank])
  @@index([organizationId, integrationId, hour])
  @@index([organizationId])
}

model ChannelAudienceHotPickBatch {
  organizationId         String
  integrationId          String
  hour                   String
  strategyId             String
  strategyVersion        Int
  materializationVersion Int
  candidateCount         Int
  pickCount              Int
  source                 String                   @default("rules")
  completedAt            DateTime
  createdAt              DateTime                 @default(now())
  updatedAt              DateTime                 @updatedAt
  organization           Organization             @relation(fields: [organizationId], references: [id])
  integration            Integration              @relation(fields: [integrationId], references: [id])
  picks                  ChannelAudienceHotPick[]

  @@id([organizationId, integrationId, hour])
  @@index([integrationId, hour])
  @@index([organizationId])
}

model ChannelAudienceHotPick {
  organizationId         String
  integrationId          String
  hour                   String
  counterpartyExternalId String
  rulesRank              Int
  finalRank              Int
  rulesReason            String
  aiRank                 Int?
  aiReason               String?
  suggestedAction        String?
  source                 String                      @default("rules")
  createdAt              DateTime                    @default(now())
  updatedAt              DateTime                    @updatedAt
  organization           Organization                @relation(fields: [organizationId], references: [id])
  integration            Integration                 @relation(fields: [integrationId], references: [id])
  batch                  ChannelAudienceHotPickBatch @relation(fields: [organizationId, integrationId, hour], references: [organizationId, integrationId, hour])
  audienceMember         ChannelAudienceMember       @relation("AudienceMemberHotPicks", fields: [integrationId, counterpartyExternalId], references: [integrationId, externalId])

  @@id([organizationId, integrationId, hour, counterpartyExternalId])
  @@index([integrationId, hour, finalRank])
  @@index([organizationId, integrationId, hour])
  @@index([organizationId])
}

model ChannelAudienceNote {
  id                     String                @id @default(cuid())
  organizationId         String
  integrationId          String
  counterpartyExternalId String
  authorUserId           String
  content                String
  createdAt              DateTime              @default(now())
  updatedAt              DateTime              @updatedAt
  organization           Organization          @relation(fields: [organizationId], references: [id])
  integration            Integration           @relation(fields: [integrationId], references: [id])
  author                 User                  @relation(fields: [authorUserId], references: [id])
  audienceMember         ChannelAudienceMember @relation("AudienceMemberNotes", fields: [integrationId, counterpartyExternalId], references: [integrationId, externalId])

  @@index([organizationId, integrationId, counterpartyExternalId, createdAt])
  @@index([integrationId, counterpartyExternalId, createdAt])
  @@index([authorUserId])
  @@index([organizationId])
}

model ChannelRelationshipGradeSnapshot {
  id                          String                @id @default(cuid())
  organizationId              String
  integrationId               String
  counterpartyExternalId      String
  windowStartedAt             DateTime
  snapshotAt                  DateTime
  effortScore                 Int
  reciprocationScore          Int
  reciprocity                 Float?
  grade                       Float?
  formulaVersion              Int                   @default(1)
  relationshipStrategyId      String?
  relationshipStrategyVersion Int?
  createdAt                   DateTime              @default(now())
  organization                Organization          @relation(fields: [organizationId], references: [id])
  integration                 Integration           @relation(fields: [integrationId], references: [id])
  audienceMember              ChannelAudienceMember @relation("AudienceMemberGradeSnapshots", fields: [integrationId, counterpartyExternalId], references: [integrationId, externalId])

  @@unique([integrationId, counterpartyExternalId, snapshotAt])
  @@index([integrationId, counterpartyExternalId, snapshotAt])
  @@index([integrationId, snapshotAt])
  @@index([organizationId])
}

enum WebhookHttpLogDirection {
  INBOUND
  OUTBOUND
}

enum WebhookHttpLogSource {
  ORG_WEBHOOK
  CHANNEL_WEBHOOK
  TEST
}

enum AdminScheduleLogKey {
  RELATIONSHIP_GRADES
  FOLLOWER_BOT_SCORES
  HOT_TRIAGE
  FOLLOWER_CULTIVATE
  LEAD_BRIDGE
  MISSING_POST_RECOVERY
  POST_WORKFLOWS
  AUTOPOST_WORKFLOWS
}

enum AdminScheduleLogLevel {
  INFO
  WARN
  ERROR
}

model PostHttpLog {
  id              String       @id @default(uuid())
  organizationId  String
  postId          String?
  integrationId   String?
  provider        String
  method          String
  url             String
  statusCode      Int?
  requestHeaders  String       @default("{}")
  requestBody     String       @default("")
  responseHeaders String       @default("{}")
  responseBody    String       @default("")
  error           String?
  createdAt       DateTime     @default(now())
  organization    Organization @relation(fields: [organizationId], references: [id])
  post            Post?        @relation(fields: [postId], references: [id], onDelete: SetNull)
  integration     Integration? @relation(fields: [integrationId], references: [id], onDelete: SetNull)

  @@index([organizationId, createdAt])
  @@index([postId])
}

model WebhookHttpLog {
  id                String                  @id @default(uuid())
  organizationId    String
  webhookId         String?
  integrationId     String?
  direction         WebhookHttpLogDirection
  source            WebhookHttpLogSource
  method            String
  url               String
  statusCode        Int?
  requestHeaders    String                  @default("{}")
  requestBody       String                  @default("")
  responseHeaders   String                  @default("{}")
  responseBody      String                  @default("")
  error             String?
  sourceDisplayName String?
  sourceUsername    String?
  targetDisplayName String?
  targetUsername    String?
  eventType         String?
  createdAt         DateTime                @default(now())
  organization      Organization            @relation(fields: [organizationId], references: [id])
  webhook           Webhooks?               @relation(fields: [webhookId], references: [id], onDelete: SetNull)
  integration       Integration?            @relation(fields: [integrationId], references: [id], onDelete: SetNull)

  @@index([organizationId, createdAt])
  @@index([webhookId])
  @@index([direction])
}

model AdminScheduleLog {
  id          String                @id @default(uuid())
  scheduleKey AdminScheduleLogKey
  level       AdminScheduleLogLevel
  message     String
  meta        String                @default("{}")
  createdAt   DateTime              @default(now())

  @@index([scheduleKey, createdAt])
}
